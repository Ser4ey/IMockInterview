# План реализации MCP/внешнего контекста и укрепления функциональности IMock

Дата: 29.05.2026

## Журнал выполнения

- Этап 0: выполнен. Baseline проверен: backend unit/API tests, frontend build, frontend e2e.
- Этапы 1-3: выполнены. Добавлены диагностические поля генерации, `question_hash`, слой внешнего контекста, подключение контекста к prompt/LLM/generation flow, обновлены backend/API/TS-типы. Проверка: `backend` unittest discover, 9 tests passed.
- Этапы 4-6: выполнены. Усилена JSON-валидация LLM-ответа, добавлен quality gate, нормализация и дедупликация вопросов через `question_hash`. Проверка: `backend` unittest discover, 12 tests passed.
- Этапы 7-8: выполнены. Admin API проставляет hash для ручных вопросов и возвращает диагностику jobs; admin UI показывает provider/context/skipped, источник и hash вопросов. Проверка: backend unittest discover, frontend build, frontend e2e passed.
- Этапы 9-10: выполнены. Пользовательский сценарий дополнительно покрыт тестами на отсутствие активных вопросов, изоляцию чужих интервью, историю/результат/прогресс. Проверка: backend unittest discover, 14 tests passed.
- Этапы 11-12: выполнены. Seed-данные создают `question_hash` для всех демо-вопросов; mock-режим и роли user/admin покрыты регрессией. Проверка: backend unittest discover, 14 tests passed.
- Этапы 13-14: выполнены. README и `.env.example` обновлены под YandexGPT и внешний Yandex/MCP-контекст; финально пройдены backend tests, frontend build, e2e и генерация скриншотов. Docker build не выполнен из-за недоступного Docker daemon в окружении.

Документ фиксирует пошаговый план доработки проекта IMock на основе текущего состояния кодовой базы. Цель - довести механизм генерации банка вопросов, сценарий интервью, администрирование, сохранение результатов и тестирование до состояния, пригодного для демонстрации и защиты ВКР.

## Карта выполнения работ

| Этап | Блок работ | Статус | Основные файлы | Проверка после этапа |
|---|---|---|---|---|
| 0 | Зафиксировать текущее состояние | Запланировано | `backend/tests/*`, `frontend/tests/e2e/*`, `README.md` | Backend tests, frontend build, e2e |
| 1 | Укрепить модель банка вопросов и jobs | Запланировано | `backend/app/models/interview.py`, `backend/app/schemas/interview.py`, Alembic | Unit/API tests |
| 2 | Вынести внешний контекст в отдельный слой | Запланировано | `backend/app/services/external_context.py`, `backend/app/core/config.py` | Unit tests provider |
| 3 | Встроить MCP/внешний контекст в генерацию вопросов | Запланировано | `question_generation.py`, `prompt_builder.py`, `llm_client.py` | Tests: source + question + job |
| 4 | Усилить валидацию JSON от модели | Запланировано | `llm_client.py`, `question_generation.py` | Tests invalid/valid JSON |
| 5 | Реализовать дедупликацию вопросов | Запланировано | `question_generation.py`, модели/миграции | Tests duplicate generation |
| 6 | Добавить quality gate для вопросов | Запланировано | `question_quality.py`, `question_generation.py` | Tests bad/good questions |
| 7 | Доработать admin API | Запланировано | `backend/app/api/endpoints/admin.py`, schemas | API tests admin workflow |
| 8 | Доработать admin UI | Запланировано | `frontend/src/pages/admin/*`, `frontend/src/api/admin.ts` | Frontend build + e2e |
| 9 | Укрепить выбор вопроса из БД во время интервью | Запланировано | `interview_engine.py`, `interviews.py` | Tests active/disabled questions |
| 10 | Проверить историю, сообщения, результат и прогресс | Запланировано | `interviews.py`, `progress.py`, result/profile pages | Backend tests + e2e |
| 11 | Проверить авторизацию user/admin | Запланировано | `deps.py`, `AdminRoute.tsx`, Header/Sidebar | Tests 403 + UI routes |
| 12 | Обновить mock-режим и seed-данные | Запланировано | `demo_seed.py`, `seed_demo.py`, mock services | Idempotent seed tests |
| 13 | Обновить README и env для MCP/Yandex | Запланировано | `README.md`, `.env.example`, `architecture.md` | Manual README checklist |
| 14 | Финальная интеграционная проверка | Запланировано | весь проект | tests/build/e2e/screenshots/Docker |

## Этап 0. Зафиксировать текущее состояние

### Работы

- Запустить текущие тесты без изменений.
- Зафиксировать, что уже работает.
- Проверить, какие сценарии покрыты тестами, а какие нет.
- Проверить текущие env-настройки LLM/Yandex/mock.
- Проверить Docker, seed, screenshots и e2e.

### Основные файлы

- `backend/tests/*`;
- `frontend/tests/e2e/imock.e2e.mjs`;
- `backend/.env.example`;
- `README.md`;
- `docker-compose.yml`.

### Тестирование

```bash
cd backend
.\venv\Scripts\python.exe -m unittest discover -s tests

cd ../frontend
npm run build
npm run test:e2e
```

### Критерий готовности

- Все текущие тесты проходят.
- Есть подтверждение, что текущий функционал не сломан перед доработками.

## Этап 1. Укрепить модель банка вопросов и jobs

### Работы

- Проверить текущую модель `QuestionGenerationJob`.
- При необходимости добавить поля:
  - `skipped_count`;
  - `provider`;
  - `context_used`;
  - `raw_response_preview` или безопасное диагностическое поле.
- Добавить нормализованный ключ или fingerprint вопроса, например `question_hash`.
- Подготовить миграцию Alembic.
- Обновить schema sync для SQLite, если он используется в проекте.

### Основные файлы

- `backend/app/models/interview.py`;
- `backend/app/schemas/interview.py`;
- `backend/alembic/versions/*`;
- `backend/app/db/schema_sync.py`.

### Тестирование

- Тест на наличие новых полей в модели.
- Тест миграции/инициализации SQLite.
- Тест, что seed-данные продолжают создаваться.

```bash
cd backend
.\venv\Scripts\python.exe -m unittest discover -s tests
```

### Критерий готовности

- База поддерживает статусы, диагностику и будущую дедупликацию генерации.
- Старые сценарии не сломаны.

## Этап 2. Вынести внешний контекст в отдельный слой

### Работы

- Добавить интерфейс внешнего контекста:
  - `ExternalContextProvider`;
  - `MockExternalContextProvider`;
  - `YandexMCPContextProvider` или `YandexSearchContextProvider`.
- Provider принимает:
  - название типа интервью;
  - роль;
  - стек;
  - уровень;
  - количество вопросов.
- Provider возвращает структурированные источники:
  - `title`;
  - `url`;
  - `snippet`;
  - `source_type`;
  - `retrieved_at`.
- В mock-режиме возвращать стабильные демонстрационные источники.
- В Yandex/MCP-режиме подготовить адаптер через env-настройки.

### Основные файлы

- новый файл `backend/app/services/external_context.py`;
- `backend/app/core/config.py`;
- `backend/.env.example`;
- `backend/app/services/question_generation.py`;
- `README.md`.

### Тестирование

- Unit-тест mock provider.
- Тест, что provider возвращает список источников.
- Тест, что при ошибке provider генерация не падает полностью или корректно пишет ошибку в job.

```bash
cd backend
.\venv\Scripts\python.exe -m unittest discover -s tests
```

### Критерий готовности

- В проекте есть явная архитектурная точка подключения MCP/Web Search.
- Генерация вопросов может использовать внешний контекст, а не только внутренний mock.

## Этап 3. Встроить MCP/внешний контекст в генерацию вопросов

### Работы

- В `QuestionGenerationService.generate_questions` перед вызовом LLM получать внешний контекст.
- Передавать найденный контекст в `PromptBuilder`.
- Сохранять источники в `QuestionSource`.
- Связывать каждый вопрос с источником.
- Если LLM вернул `source_title/source_url`, использовать эти данные.
- Если LLM источник не вернул, привязывать вопрос к источнику из provider.

### Основные файлы

- `backend/app/services/question_generation.py`;
- `backend/app/services/prompt_builder.py`;
- `backend/app/services/llm_client.py`;
- `backend/app/models/interview.py`;
- `backend/app/schemas/interview.py`.

### Тестирование

- Тест: генерация создаёт `QuestionGenerationJob`.
- Тест: генерация создаёт `QuestionSource`.
- Тест: генерация создаёт `Question`.
- Тест: вопрос связан с источником.
- Тест: mock provider работает без интернета.

```bash
cd backend
.\venv\Scripts\python.exe -m unittest discover -s tests
```

### Критерий готовности

- Механизм генерации использует внешний контекст/MCP-слой.
- Источники вопросов сохраняются и доступны в admin API.

## Этап 4. Усилить валидацию JSON от модели

### Работы

- Проверять, что LLM вернул JSON-массив.
- Проверять обязательные поля:
  - `question_text`;
  - `level`;
  - `tags`;
  - `expected_answer`;
  - `evaluation_criteria`;
  - `source_title/source_url`.
- Проверять содержательность:
  - `question_text` не слишком короткий;
  - `expected_answer` не пустой и достаточно подробный;
  - `evaluation_criteria` содержит минимум 2-3 критерия;
  - `level` совпадает с запрошенным;
  - `tags` не содержат пустых строк.
- При некорректном JSON:
  - job получает `failed`;
  - ошибка пишется в `error_message`;
  - некорректные вопросы не сохраняются.

### Основные файлы

- `backend/app/services/llm_client.py`;
- `backend/app/services/question_generation.py`;
- `backend/tests/test_llm_and_seed.py`;
- новый тестовый файл, например `backend/tests/test_question_generation_validation.py`.

### Тестирование

- Тест валидного JSON.
- Тест невалидного JSON.
- Тест JSON с Markdown fences.
- Тест JSON без criteria.
- Тест JSON с неправильным level.
- Тест, что при ошибке job становится `failed`.

```bash
cd backend
.\venv\Scripts\python.exe -m unittest discover -s tests
```

### Критерий готовности

- Плохой ответ модели не ломает банк вопросов и корректно отражается в status job.

## Этап 5. Реализовать дедупликацию вопросов

### Работы

- Добавить функцию нормализации текста вопроса:
  - lower;
  - trim;
  - убрать лишние пробелы;
  - убрать финальные знаки пунктуации.
- Считать hash/fingerprint.
- Проверять дубликаты:
  - внутри одного ответа LLM;
  - среди существующих вопросов для `interview_type_id + level`;
  - независимо от регистра.
- Дубликаты не сохранять.
- В job записывать:
  - `generated_count`;
  - `skipped_count`, если поле добавлено;
  - диагностическое сообщение при необходимости.

### Основные файлы

- `backend/app/services/question_generation.py`;
- `backend/app/models/interview.py`;
- `backend/app/schemas/interview.py`;
- Alembic migration;
- tests.

### Тестирование

- LLM/mock возвращает 3 одинаковых вопроса.
- В БД сохраняется только 1.
- Повторный запуск генерации не создаёт дубликаты.
- Job показывает корректное количество сохранённых и пропущенных вопросов.

```bash
cd backend
.\venv\Scripts\python.exe -m unittest discover -s tests
```

### Критерий готовности

- Банк вопросов не засоряется повторами.

## Этап 6. Добавить quality gate для expected_answer и evaluation_criteria

### Работы

- Добавить сервис `QuestionQualityService`.
- Проверять:
  - длину вопроса;
  - длину эталонного ответа;
  - количество критериев;
  - отсутствие пустых критериев;
  - отсутствие слишком общих критериев;
  - соответствие уровня.
- Плохие вопросы:
  - пропускать;
  - записывать причину в job;
  - не сохранять как активные.

### Основные файлы

- новый файл `backend/app/services/question_quality.py`;
- `backend/app/services/question_generation.py`;
- tests.

### Тестирование

- Тест хорошего вопроса.
- Тест пустого `expected_answer`.
- Тест одного слишком общего критерия.
- Тест, что плохой вопрос не активируется.
- Тест, что job не падает, если часть вопросов плохая.

```bash
cd backend
.\venv\Scripts\python.exe -m unittest discover -s tests
```

### Критерий готовности

- В банк попадают только пригодные для демонстрации и интервью вопросы.

## Этап 7. Доработать admin API под полный workflow генерации

### Работы

- Обновить `QuestionGenerationJobRead`.
- Возвращать в API:
  - `generated_count`;
  - `skipped_count`;
  - `status`;
  - `error_message`;
  - `provider`;
  - данные об использованном контексте, если безопасно.
- Проверить:
  - обычный пользователь получает `403`;
  - admin может запускать генерацию;
  - admin видит историю jobs.
- Оставить генерацию синхронной для ВКР, если это проще и стабильнее.

### Основные файлы

- `backend/app/api/endpoints/admin.py`;
- `backend/app/schemas/interview.py`;
- `frontend/src/api/admin.ts`.

### Тестирование

- Backend API test:
  - user denied;
  - admin creates type;
  - admin runs generation;
  - admin sees jobs;
  - failed job returns error.

```bash
cd backend
.\venv\Scripts\python.exe -m unittest discover -s tests
```

### Критерий готовности

- Admin API полностью отражает состояние генерации.

## Этап 8. Доработать admin UI

### Работы

- В `AdminInterviewTypes.tsx`:
  - кнопка "Сгенерировать вопросы";
  - выбор уровня;
  - количество вопросов.
- В `AdminQuestions.tsx`:
  - показать источник;
  - показать criteria;
  - показать enabled/disabled;
  - фильтры.
- В `AdminGenerationJobs.tsx`:
  - статус;
  - requested/generated/skipped;
  - ошибка;
  - provider.
- Добавить понятные empty/error states.

### Основные файлы

- `frontend/src/pages/admin/AdminInterviewTypes.tsx`;
- `frontend/src/pages/admin/AdminQuestions.tsx`;
- `frontend/src/pages/admin/AdminGenerationJobs.tsx`;
- `frontend/src/api/admin.ts`;
- `frontend/src/types/interview.ts`.

### Тестирование

```bash
cd frontend
npm run build
npm run test:e2e
```

Дополнительно проверить e2e-сценарий:

- login admin;
- открыть `/admin/interview-types`;
- запустить генерацию;
- открыть `/admin/questions`;
- увидеть вопросы;
- открыть `/admin/question-generation-jobs`;
- увидеть job.

### Критерий готовности

- На защите можно показать полный admin workflow без Swagger.

## Этап 9. Проверить и укрепить выбор вопроса из БД во время интервью

### Работы

- Проверить, что disabled-вопросы не попадают в интервью.
- Проверить, что вопрос связывается с `Message.question_id`.
- Проверить, что при переходе к следующему вопросу не повторяется предыдущий.
- Оставить текущую модель `InterviewSession.current_question_id` + `Message.question_id`, если её достаточно для ВКР.
- Добавить тест на disabled question.
- Добавить тест, что первое AI-сообщение содержит вопрос из банка.

### Основные файлы

- `backend/app/services/interview_engine.py`;
- `backend/app/api/endpoints/interviews.py`;
- `backend/tests/test_api_flows.py`.

### Тестирование

- Создать тип интервью.
- Создать активный и отключённый вопрос.
- Начать интервью.
- Убедиться, что выбран только активный вопрос.
- Отправить ответ.
- Убедиться, что `question_id` сохранён.

```bash
cd backend
.\venv\Scripts\python.exe -m unittest discover -s tests
```

### Критерий готовности

- Сценарий интервью строго опирается на банк вопросов.

## Этап 10. Проверить сохранение истории, сообщений, результата и прогресса

### Работы

- Проверить endpoints:
  - `GET /interviews`;
  - `GET /interviews/history`;
  - `GET /interviews/{id}/messages`;
  - `GET /interviews/{id}/result`;
  - `GET /progress`.
- Проверить user isolation:
  - пользователь A не видит интервью пользователя B.
- Проверить, что progress не содержит коммерческих полей.
- Проверить, что результат создаётся один раз, а не дублируется.

### Основные файлы

- `backend/app/api/endpoints/interviews.py`;
- `backend/app/api/endpoints/progress.py`;
- `backend/app/services/interview_engine.py`;
- `frontend/src/pages/ProfilePage.tsx`;
- `frontend/src/pages/InterviewResult.tsx`.

### Тестирование

- Backend tests:
  - history;
  - messages;
  - result;
  - progress;
  - forbidden для чужой session.
- Frontend e2e:
  - пройти интервью;
  - открыть результат;
  - открыть профиль/прогресс.

```bash
cd backend
.\venv\Scripts\python.exe -m unittest discover -s tests

cd ../frontend
npm run test:e2e
```

### Критерий готовности

- Данные интервью сохраняются и доступны только владельцу.

## Этап 11. Проверить авторизацию user/admin

### Работы

- Проверить backend dependency `get_current_admin_user`.
- Проверить frontend `AdminRoute`.
- Проверить sidebar/header:
  - admin видит admin-раздел;
  - user не видит admin-раздел.
- Проверить прямой заход user на `/admin/*`.

### Основные файлы

- `backend/app/api/deps.py`;
- `frontend/src/components/AdminRoute.tsx`;
- `frontend/src/components/Sidebar.tsx`;
- `frontend/src/components/Header.tsx`;
- `frontend/tests/e2e/imock.e2e.mjs`.

### Тестирование

- Backend:
  - user получает `403` на admin endpoints.
- Frontend:
  - user не видит admin navigation;
  - admin видит admin navigation;
  - direct URL для user закрыт.

```bash
cd backend
.\venv\Scripts\python.exe -m unittest discover -s tests

cd ../frontend
npm run test:e2e
```

### Критерий готовности

- Роли разделены и на backend, и на frontend.

## Этап 12. Mock-режим и seed-данные

### Работы

- Обновить mock provider внешнего контекста.
- Обновить mock LLM при необходимости.
- Проверить seed:
  - admin;
  - user;
  - Backend Java;
  - Frontend React;
  - вопросы Junior/Middle/Senior.
- Сделать seed идемпотентным:
  - повторный запуск не плодит дубликаты.

### Основные файлы

- `backend/app/services/demo_seed.py`;
- `backend/scripts/seed_demo.py`;
- `backend/app/services/llm_client.py`;
- `backend/app/services/external_context.py`.

### Тестирование

```bash
cd backend
.\venv\Scripts\python.exe scripts\seed_demo.py
.\venv\Scripts\python.exe scripts\seed_demo.py
.\venv\Scripts\python.exe -m unittest discover -s tests
```

### Критерий готовности

- Демонстрационные данные стабильны для защиты.

## Этап 13. README и env для MCP/Yandex

### Работы

- Обновить README:
  - как включить mock;
  - как включить YandexGPT;
  - как включить MCP/Web Search provider;
  - какие env нужны;
  - что делать, если Yandex/MCP недоступен.
- Обновить `.env.example`:
  - `LLM_MODE=mock|yandex`;
  - `EXTERNAL_CONTEXT_MODE=mock|yandex_mcp`;
  - `YANDEX_API_KEY`;
  - `YANDEX_FOLDER_ID`;
  - MCP/search-specific переменные, если нужны.
- Добавить описание fallback-режима.

### Основные файлы

- `README.md`;
- `backend/.env.example`;
- `architecture.md`.

### Тестирование

- По README поднять проект локально.
- Проверить mock-режим.
- Проверить, что без Yandex токена проект не падает.
- Проверить, что с `LLM_MODE=mock` проходит e2e.

```bash
cd frontend
npm run test:e2e
```

### Критерий готовности

- Проект можно запустить по документации без устных пояснений.

## Этап 14. Финальная интеграционная проверка

### Работы

- Запустить backend tests.
- Запустить frontend build.
- Запустить e2e.
- Запустить screenshots.
- Проверить Docker build/compose.
- Проверить отсутствие тарифов/подписок.
- Проверить ключевые страницы вручную:
  - admin interview types;
  - admin questions;
  - generation jobs;
  - dashboard;
  - chat;
  - result;
  - profile/progress.

### Команды проверки

```bash
cd backend
.\venv\Scripts\python.exe -m unittest discover -s tests

cd ../frontend
npm run build
npm run test:e2e
npm run screenshots:vkr
```

Docker:

```bash
docker compose build
docker compose up
```

Проверка отсутствия коммерческой логики:

```bash
rg -n -i "tariff|subscription|pricing|payment|billing|premium|free|pro|тариф|подпис|оплат|премиум" backend frontend README.md
```

### Критерий готовности

- Все тесты проходят.
- Скриншоты обновлены.
- Проект можно показать на защите.
- MCP/внешний контекст архитектурно подключён.
- Mock-режим остаётся рабочим.
- Коммерческой логики нет.

## Итоговый порядок реализации

1. Зафиксировать зелёное состояние проекта.
2. Усилить БД/jobs под dedupe и диагностику.
3. Добавить `ExternalContextProvider` под MCP/Web Search.
4. Встроить внешний контекст в генерацию вопросов.
5. Усилить JSON validation.
6. Добавить dedupe.
7. Добавить quality gate.
8. Доработать admin API.
9. Доработать admin UI.
10. Укрепить interview engine и связь `question_id`.
11. Укрепить history/result/progress/user isolation.
12. Обновить seed/mock.
13. Обновить README/env.
14. Финально прогнать tests/build/e2e/screenshots/Docker.

## Правило выполнения

После каждого этапа обязательно запускаются проверки этапа. Следующий этап начинается только после того, как тесты текущего этапа зелёные или найденный дефект исправлен и проверка повторена.
