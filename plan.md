# План разработки проекта IMock

Этот план составлен с учетом методологии **NoDemo** (код сразу готов к продакшену) и принятых технических решений:
*   **Backend:** Python, FastAPI, SQLite (Repository Pattern), JWT, Yandex Cloud AI.
*   **Frontend:** TypeScript, React, Material UI (MUI).
*   **Comms:** REST API + Short Polling.
*   **Infra:** Docker Compose.

---

## Этап 0: Инициализация и Инфраструктура (Выполнено)
*Цель: Подготовить почву для параллельной работы бэкенд и фронтенд разработчиков.*

1.  **Настройка репозитория:** (Выполнено)
    *   [x] Создать структуру монорепозитория: `/backend`, `/frontend`.
    *   [x] Настроить `.gitignore` (частично в подпапках).
2.  **Backend Setup:** (Выполнено)
    *   [x] Инициализация requirements.txt.
    *   [x] Базовая настройка FastAPI приложения (main.py, config.py).
    *   [x] Написание Dockerfile.
3.  **Frontend Setup:** (Выполнено)
    *   [x] Инициализация Vite + React + TypeScript.
    *   [x] Установка и базовая настройка Material UI.
    *   [x] Написание Dockerfile.
4.  **Docker Environment:** (Выполнено)
    *   [x] Написание `docker-compose.yml` для локальной разработки.

## Этап 1: Ядро Backend и Аутентификация (Выполнено)
*Цель: Реализовать безопасный доступ и работу с БД.*

1.  **Архитектура БД:**
    *   [x] Настройка SQLAlchemy (Async engine) и Alembic для миграций.
    *   [x] Реализация базового класса `BaseRepository` (CRUD операции).
2.  **Модели пользователей:**
    *   [x] Создание модели `User` (email, password_hash, tariff, requests_count).
    *   [x] Миграция БД.
3.  **Модуль Auth:**
    *   [x] Реализация утилит для хеширования паролей.
    *   [x] Реализация JWT (выпуск access/refresh токенов).
    *   [x] Эндпоинты: `/auth/register`, `/auth/login`, `/auth/me`, `/auth/refresh`.
    *   [x] Dependency `get_current_user` для защиты роутов.

## Этап 2: Ядро Frontend и UI-кит (Выполнено)
*Цель: Сделать "скелет" приложения, чтобы можно было "натягивать" функционал.*

1.  **Базовый UI Layout:**
    *   [x] Header (Лого, Навигация, Профиль/Кнопка входа).
    *   [x] Sidebar (для навигации в личном кабинете).
    *   [x] Footer.
2.  **Маршрутизация и Auth Context:**
    *   [x] Настройка React Router.
    *   [x] Реализация `AuthProvider` (хранение токена, проверка авторизации, редиректы).
    *   [x] Страницы: Landing, Login, Register, Dashboard (пустая).
3.  **API Client:**
    *   [x] Настройка Axios инстанса (интерсепторы для добавления Bearer токена и авто-обновления токена).

## Этап 3: Интеграция с ИИ (Backend) (1 запрос)
*Цель: Научить бэкенд общаться с "мозгами" проекта.*

1.  **AI Abstraction Layer:**
    *   Создание абстрактного базового класса `AIService`.
    *   Реализация конкретного класса `YandexGPTService`.
    *   Интеграция Yandex Cloud SDK.
2.  **Prompt Engineering (System Prompts):**
    *   Метод `generate_interview_questions(position, level)` -> JSON list.
    *   Метод `conduct_interview_step(chat_history, message)` -> AI response.
    *   Метод `analyze_interview(chat_history)` -> Feedback report.

## Этап 4: Функционал Собеседований (Backend + Frontend) (1 запрос)
*Цель: Реализовать основной бизнес-процесс.*

### Backend
1.  **Модели Чатов:**
    *   Таблицы `Chat` (topic, status, user_id) и `Message` (role, content, timestamp).
    *   Репозитории `ChatRepository`, `MessageRepository`.
2.  **Бизнес-логика (Service Layer):**
    *   Создание чата: генерация приветствия от ИИ.
    *   Отправка сообщения: сохранение User msg -> запрос к AI -> сохранение AI msg.
3.  **API Эндпоинты:**
    *   `POST /chats` (создать).
    *   `GET /chats` (список).
    *   `GET /chats/{id}/messages` (история).
    *   `POST /chats/{id}/messages` (отправить).

### Frontend
1.  **Интерфейс Списка чатов:**
    *   Отображение карточек собеседований со статусами.
    *   Кнопка "Начать новое собеседование" (модалка с выбором темы/грейда).
2.  **Интерфейс Чата:**
    *   Компонент списка сообщений (пузыри сообщений User/AI).
    *   Input field для ввода ответа.
    *   **Реализация Polling:** `useInterval` хук для опроса новых сообщений каждые 3 сек.

## Этап 5: Финализация и "Плюшки" (1 запрос)
*Цель: Довести проект до состояния готового продукта.*

1.  **Статистика и Лимиты:**
    *   Backend: Подсчет использованных токенов/запросов. Логика ограничения тарифов.
    *   Frontend: Вывод статистики в профиле.
2.  **Анализ результатов:**
    *   Страница результатов собеседования (отображение фидбека от ИИ).
3.  **Полировка UI/UX:**
    *   Обработка ошибок (Error Boundaries, Toasts notifications).
    *   Лоадеры и скелетоны при загрузке данных.

## Этап 6: Тестирование и Сдача (1 запрос)
1.  Ручное E2E тестирование сценариев.
2.  Финальный код-ревью.
3.  Подготовка документации (заполнение README.md как запустить).
